# Server Configuration
PORT=3001
NODE_ENV=development # or 'production'

# Security - IMPORTANT: Change this in your actual .env file or environment
JWT_SECRET="your-super-strong-and-long-random-jwt-secret-key"
JWT_EXPIRES_IN="1d"

# Database (SQLite path is configured in models/index.js based on NODE_ENV)
# Example for PostgreSQL (if you switch from SQLite)
# DB_DIALECT=postgres
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=youruser
# DB_PASSWORD=yourpassword
# DB_NAME=mediaserver_db
